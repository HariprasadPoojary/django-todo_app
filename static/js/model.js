"use strict";

const api_url = "http://127.0.0.1:8000/api";

export const loadTasks = async function (userId) {
    try {
        const data = await getJSON()
	} catch (error) {
		console.log(`${error} 💢💢`);
		throw error;
	}
};
